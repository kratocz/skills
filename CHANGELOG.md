# Changelog

Notable, user-visible changes to the skills in this collection, grouped by the date they landed on `main` and prefixed with the affected skill (or `repo` for collection-wide changes). Mechanical noise — typos, refactors without behavior change — is omitted; the complete history of a single skill is `git log -- skills/<name>/`.

## 2026-09-04

### Added

- **task-delivery:** new skill — carry a single tracker task from ready-to-start to closed, reading branch naming, gate set, PR body shape, label flow and tracker conventions from the project's AGENTS.md instead of hard-coding them. Delegates the review round to `code-review` rather than restating it, and concentrates on the parts that actually go wrong: reading the parent epic for the scope boundary before implementing, batching schema- and interface-level decisions into one question round while they are still cheap, and two rules about gates learned from shipping a lint failure — a result is valid only for the tree it ran on, and a result read from a tailed output is not a result. Encodes three integration traps: `gh pr merge --delete-branch` fails locally from a worktree *after* the merge has landed and must never be retried, GitHub refuses `Approve` on your own pull request so a self-review must say out loud that nobody signed off, and a merge needs an imperative in the user's own message — a dialog selection is not authorization. A finding whose fix belongs downstream gets a home: reasoning at the surprising line, requirement plus QA step on the next task.
- **epic-breakdown:** new skill — turn a written work breakdown into a tracker epic with numbered subtasks and formal, transitively reduced dependency edges, reading the prefix scheme, subtask shape and what a phase number *means* from the project's AGENTS.md instead of hard-coding them. Its centre is a granularity test applied before anything is created — one subtask is one pull request with its own QA steps; a title that needs a "+", or QA steps that split into two independently passing groups, is two subtasks; a design-document line is not a task boundary — because the founding occasion (an eight-subtask cut bundled by proposal line, rejected in full and re-founded as twenty the next day) showed that the coarse cut looks tidy for an hour and costs a delete-and-recreate after that. Reads the tracker before the document: review follow-ups already filed under an older prefix are referenced or updated rather than duplicated, and a subtask someone has started is cut around, not restructured. Attaches a hard sequencing constraint to the subtasks that *produce* the constrained artifact rather than to the first one in their area, names every edge that leaves the epic, lists the dependency-free starters the coarse cut had hidden, shows the whole table for approval before founding, and rewrites the epic body afterwards so it does not go on saying "only the first task exists". Counterpart of `dependency-diagrams`, which reads the graph this writes.

### Changed

- **retro:** Phase 0 gains two steps and area A one rule, all three from runs that went wrong. Memory sets past roughly 40 files or 100 KB are now read by a briefed read-only subagent with a fixed output shape instead of inline — a 119-file, 328 KB set would have cost about 90k tokens of context to read directly. The write target is chosen after `git fetch` and audited against `origin/main` rather than the working tree, because a checkout ten commits behind produces findings upstream has already fixed, and a dirty or behind default branch gets a fresh worktree so the user's in-progress files are never swept up. And the area A verdict "this fact is not already in AGENTS.md" must be re-verified immediately before the write lands: with parallel sessions the target file moves mid-retro, and one run nearly shipped a file contradicting itself because another agent added the same two setup steps between analysis and merge.

- **skillset-adopt:** Phase 1 gains a pre-step for when the input is a list of skill names rather than repo URLs (a curated list or social-media post with unexpandable redirect links): resolve each name to its canonical `<owner>/<repo>` on GitHub before cloning, and pick by owner and stars because a popular skill attracts high-star forks, mirrors and re-uploads that shadow the original.

## 2026-09-03

### Added

- **mail-catchup:** new skill — catch up on an IMAP mailbox served by the zerolib-email MCP (mcp-email-server): list unread, read the relevant threads without flipping read flags, brief per thread what is asked of the user and by when — quoting deadlines or stating there are none, and checking Sent before assuming an earlier draft ever went out (the case that prompted it: a draft from June that nobody noticed had never left the mailbox). A reply goes through a dated draft file, explicit approval, an in-thread send, and a look at Sent before it is called sent. Carries the transport trap that makes the verification necessary: the server crashes on a subject with diacritics *before* SMTP, so nothing is sent and nothing shows in Sent — the subject is passed pre-encoded as an RFC 2047 encoded-word, and a new pattern is first sent to the user's own address. Gmail is deliberately out of scope; that is gmail-compose.
- **oponentura:** new skill — an adversarial pass over a written analysis, run by a subagent in a fresh context under a *refute* brief rather than a *check* brief, because "check" comes back with "looks fine" and "refute" comes back with the three claims that do not survive their own sources. The brief names its targets explicitly: every figure against the primary source, the strongest counter-argument including replications and expressions of concern the document predates, and — the one every pass so far has caught — whether the document holds its preferred explanation to the same evidential bar as the one it rejects. Findings come back by severity with the quote, the truth and the URL; the author reproduces the top findings before applying any (reviewers overreach too), records date, counts and the strongest counter-argument in the document header, and runs a scoped pass over anything added afterwards. The case that prompted it: two notes in a psychology knowledge base whose first drafts each had the central thesis partly wrong in ways the author could not see from inside the same context.

### Changed

- **debate-reply:** section 5 gains a hostile-reader pass — re-read the draft as an opponent who reads every concession as a confession; a concession that stands alone in its sentence is a quote for their audience, so its limit or consequence goes in the same sentence and consequences are spelled out rather than left to be inferred; cut whole paragraphs, never the fork half of a sentence; and when the opponent supplies the hook, an unsent symmetry thread merges into this reply instead of waiting for its own. Prompted by a devil's-advocate pass in which three of the four sentences attacked were standalone concessions.
- **kodex:** rule 5 now says what to hand the refuter — the artifact and the contract it has to satisfy, never the reasoning that produced it, because a reviewer given conclusions returns validation of those conclusions. Borrowed from `doubt-driven-development` in [addyosmani/agent-skills](https://github.com/addyosmani/agent-skills) (MIT) — the one mechanism there that this setup did not already have. English `SKILL.md` and Czech `references/kodex-cs.md` move together.

### Fixed

- **skillify:** when a project-specific skill is scaffolded, the skill invoked `superpowers:writing-skills` "as guidance for writing a quality skill" — which also pulls in that skill's Iron Law: no skill without a failing subagent baseline test first, and "write skill before testing? Delete it." Sized for standalone authoring, that rule is wrong for the case skillify is mostly used in: a workflow the session has just executed end to end, where the execution *is* the baseline. Following it literally either burns a test cycle on a project-local skill or deletes the thing just written; ignoring it silently leaves no record of why. The guidance now scopes writing-skills to naming, description and structure, and asks for the in-session baseline to be stated in the summary instead of the cycle being run or quietly skipped.
- **retro:** Phase 0 now confirms the checkout is on the integration branch and current with it before writing — fast-forwarding a clean, strictly-behind checkout, or reading the target file from `origin/main` when it has diverged — so a retro no longer lands on a stale copy. The doc-staleness sweep also excludes nested git worktrees, which a multi-session project can hold many of, each a full copy of every doc. (v0.4.1)

## 2026-09-01

### Added

- **client-questions:** new skill — prepare the question list for a meeting with a client or other party who owes the project decisions and data. Sweeps three sources in parallel, and insists on the third because it is the one that gets skipped: the previous question set usually lives in a chat message to the PM rather than anywhere committed, so without it half the list goes out twice. Triages into live / deliberately parked / already answered and keeps all three in the written output — the parked ones each carrying the cost of parking in one clause, so deferring stays a visible decision rather than a risk that quietly disappears, and the answered ones because re-asking for something the other party has explicitly said it cannot supply reads as not listening. Ranks by what the answer is worth, naming the class that is easiest to miss: questions where trying it out cannot reveal the answer, because the failure mode is a *successful* operation with wrong results — the case that prompted it was a duplicate payroll import that is accepted and quietly pays someone twice, which no amount of iterating against the real system would surface. Also reads the other party's own sample data back to them (the same field encoded two ways, a supported variant that never appears, a stale date), and splits what is per-platform from what is per-installation so a vendor's consultant is not asked for a customer's code list.

## 2026-08-27

### Added

- **debate-reply:** new skill — answer a public challenge to a claim the user has already written down, in the order that keeps the durable artifact ahead of the ephemeral one: correct the note, then draft the reply, then archive. Forces a steelman before any rebuttal, because a refutation that only beats the popular version of a position invites the reply "you didn't address X" and loses the exchange on procedure — the case that prompted it turned on a genuine grammatical detail (a shift from singular to plural agent) that the note had never mentioned. Insists on raw primary text over summaries after a summarizing fetch of a scripture site returned a paraphrase that silently dropped the exact clause the argument rested on, and on quoting the opponent's own edition. Names two moves that generalize well beyond the originating topic: **the dial** — when a claim is asserted as precise where that flatters it and loose where precision would refute it, every setting that saves it from falsehood costs the evidential force it was invoked for — and **symmetry**, offering the same falsification rule to both sides before anyone knows the outcome, which converts a dispute into a dated bet or exposes the double standard. Carries the discipline that a long public reply is easiest to get wrong on attribution, and that attacking the opponent's tradition turns a solvable dispute into an identity fight.

### Fixed

- **init-project:** the procedure opened with "always operate in the user's current `pwd` — never `cd` elsewhere", which has no path for the common ask "založ projekt v `../foo`" where the target is a sibling directory that does not exist yet. Following it literally meant either bootstrapping in the wrong directory or breaking the skill's own rule. The target directory is now a first-class concept, defaulting to `pwd` but created with `mkdir -p` when the user names one, worked against via absolute paths or `git -C` because some harnesses reset the shell's working directory between calls, and echoed back as an absolute path so a mistyped relative path surfaces before a repo is created in the wrong place.

## 2026-08-26

### Added

- **skillset-adopt:** new skill — decide what to take from a third-party skill collection, and take it. Inventories the existing setup *before* reading any candidate (the CLI lock file for what came from where, the plugin marketplace cache for skills the lock never sees), because novelty is relative to that inventory and part of the candidate repo is often already installed from an earlier cherry-pick. Separates the two failure modes that a title-level comparison misses: a **name collision**, where both skills resolve to `~/.agents/skills/<name>` and the install silently replaces an incumbent symlinked into the user's own clone, and a **trigger overlap**, where nothing is overwritten and the two descriptions simply compete — which calls for a swap, not an addition. Flags collections that are a coupled workflow spine rather than a toolbox, including the sharp case of a second always-on discipline contradicting one already in place. Ends in a three-way verdict per skill — install / borrow the idea (licence permitting) / skip — and verifies the install from the filesystem and lock file rather than the command's output. Complements `find-skills` (capability-driven discovery of an unknown skill) and mirrors `mcp-server-adopt`.
- **skill-dry-run:** new skill — prove an already-written skill works by executing it end to end on real data in a clean context. Fingerprints every target the skill can write to (count *and* highest id, so a create-plus-delete cannot hide), removes the credential path instead of only forbidding the write, dispatches a fresh-context agent with a prompt that names what to check but never the expected answer, diffs the fingerprint afterwards rather than trusting the agent's own report, and re-verifies each returned finding against the file — one such report named a broken `awk` line that was correct in the source. Also extracts the SKILL.md's embedded snippets and runs them against real input, which is how a field read as `content` instead of `aiTitle` and a `date -u -j -f` that parses local wall-clock as UTC were both caught. Distinct from `superpowers:writing-skills`, which pressure-tests whether an agent complies with a skill's wording while it is being authored (v1.0.0).

### Fixed

- **work-reconcile:** the coverage ratio divided wall-clock overlap by the gap-capped estimate — different units, so it exceeded 1.0 for any block with long internal pauses (23 of 89 on a live run) and the remainder went negative. Scaling by the covered fraction of the span instead assumes activity is spread evenly and proposed 29.2 h onto a window already holding 33.3 h. The step now re-runs the gap-capping over just the timestamps outside the busy intervals: it measures the un-logged work rather than inferring it, is bounded in [0,1] by construction, and yields 49 COVERED / 16.2 h proposed on that window (v0.9.0).
- **work-reconcile:** the matching rule introduced in v0.8.0 contradicted its own example — "equality after replacing `-`/`_` with nothing" cannot match `payroll-system-PMA` to `NTIT/PMA`, since folding gives `payrollsystempma`. Read literally it left **96 of 97 blocks unattributed**, including 31 h of the project the example names, so the worktree fix landed as "no wrong answer, and no right one either". Specified properly: normalise both sides, then match on equality or on the key being a trailing separator-delimited component — which pairs `payroll-system-PMA` with `NTIT/PMA` while still refusing `webserver` → `server` (v0.9.0).
- **work-reconcile:** the repo-name fallback sat next to "no match means no project" and read as though it assigned one. It is now stated as simply the last segment to try (v0.9.0).
- **work-reconcile:** step 7 told the agent both to drop COVERED blocks and never to drop them silently. Harmless while cross-project collisions were rare, but once unmatched blocks compare against the all-projects bucket every COVERED block is one — 32 blocks, 50 h, on a live run. Resolved with a one-line roll-up per (project, day), expandable on request, instead of either extreme (v0.9.0).
- **work-reconcile:** the note added with the Free-flag rule claimed it caught a cinema showing. It did not — that event carries no `transparency` field at all and no keyword matched it, so 150 minutes were proposed as billable; two events had been conflated. The claim is corrected in place, and calendar events with no other attendee that the user organised now carry a `'solo?'` mark for confirmation rather than being filtered either way: solo focus time and a cinema ticket are the same shape to every automatic rule. Over one month that test separated every unambiguous work event from every unambiguous personal one, 6 for 6 (v0.9.0).
- **work-reconcile:** the git sweep was scoped to "the current repo", which is almost never where the window's work happened — measured from a checkout of this collection, the current repo held 0 in-window commits while 131 existed across seven others, so the source contributed nothing at all. The repo list is now derived from the session blocks' own working directories, with skipped paths reported so a missing clone reads as "unconfirmed" rather than "no work" (v0.9.0).
- **work-reconcile:** the ClickUp confirmatory query asked for "tasks updated in the window", which `clickup_filter_tasks` cannot express — it ranges only over `due_date_*` and `date_closed_*`, and every run rediscovered that. Replaced with what the tool actually supports, and the limitation is now stated once (v0.9.0).
- **work-reconcile:** three smaller gaps the same run surfaced — the slug encoding maps both `/` and `.` to `-` and so cannot be reversed by guessing; the per-day sanity gate did not say whether calendar blocks count toward it (they do not, or meeting-heavy days would raise their own threshold); and the review had no ordering rule for the unattributed group, which sorted last and buried a 28-row group under a single 5-minute row (v0.9.0).
- **work-reconcile:** removing the project fallback (v0.6.0) broke the coverage diff. Step 7 buckets the busy map by `(project, day)`, so once unmatched blocks started arriving with `project=null` they compared against an empty bucket and no tracked entry could ever mask them — every one was proposed as missing. A four-day window already carrying 33.3 h of tracked time produced **zero** COVERED blocks and proposed 61.6 h on top. Null-project blocks now compare against an all-projects per-day bucket; on that same window, COVERED goes 0 → 28 and the proposal 61.6 h → 9.3 h (v0.8.0).
- **work-reconcile:** the project matcher's "matches a project name case-insensitively" was ambiguous enough that two readings disagreed on live data — equality left `payroll-system-PMA` unmatched against `NTIT/PMA`, substring made the segment `cz` match five `*.cz` projects at once. Specified: equality on the project name's last `/`-separated part, separators folded, ties treated as no match (v0.8.0).
- **work-reconcile:** a git worktree's own name decided the project. `…/payroll-system-PMA/.claude/worktrees/monitoring` resolved to the Toggl project `Monitoring` — a different client's — for 12 blocks, and because the match *succeeded*, no `'project?'` gate fired to surface it. Skipping the literal `worktrees` token was not enough; a path is now truncated at `/.claude/worktrees/` and matched only to the left of it (v0.8.0).
- **work-reconcile:** the calendar filter ignored the event's Free/busy flag, so a cinema showing (150 min) and four grocery-courier slots were proposed as billable time — they matched no `exclude_keywords`, and nobody had thought to add those words. Events with `transparency: "transparent"` are now dropped: that flag is the user's own statement that the slot is not work, and it needs no keyword list to maintain (v0.8.0).
- **work-reconcile:** the git query filtered on **commit** date (`--since`/`--until`) while recording the **author** date (`%aI`) as the hit's timestamp. Rebasing and amending move one and not the other — 7 of 62 commits in one repo's four-day window diverged — so commits landed outside the reconcile window; one authored `2026-07-27 02:00` surfaced in a window starting `2026-07-28`. Git has no author-date range filter, so the query now over-fetches on commit date (`--since` padded back 7 days, no `--until`) and filters on `%aI` itself. Verified on that repo: the stray commit is gone and nothing legitimate was lost (v0.7.0).
- **work-reconcile:** the duration sanity gate was per block only, at ~6 h, which the real inflation walks straight past — it arrives as dozens of small blocks across parallel repos, none individually long. Four July days reconstructed to 19–30 h each with no single block over 6 h. A per-day gate now marks every block of a day over ~10 h with `'day?'`, and the review announces such a day once at the head of its group with its reconstructed total against what the tracker already holds. Scoped deliberately as a backstop: where the tracker holds a normal day's entries the step 7 coverage diff already absorbs the inflation (30.3 h reconstructed against 10.6 h logged proposed 1.6 h); the exposure is the thinly-logged day, where 12.6 h reconstructed against 1.0 h logged proposed 8.7 h (v0.7.0).
- **work-reconcile:** an unmatched block fell back to `sources.toggl.project_id` before the `'project?'` gate could fire, so with a non-null id configured that gate was unreachable and every unattributed block was silently assigned. On a live config that put **55.4 of 68.4 proposed hours onto a billable client project**, 14.6 h of them from a personal repo. The fallback is gone: no match now means `project=null` plus a `'project?'` mark, and the review offers to set a whole group's project in one answer so the gate does not cost a keystroke per row (v0.6.0).
- **work-reconcile:** `ai-title` lines carry the title in `aiTitle`, not `content`. Reading only `content` returned None on every Claude Code session (550 of 567 files carry `aiTitle`), so every proposal degraded to the "Práce v <dir>" placeholder and the review table was unreadable (v0.5.1).
- **work-reconcile:** the window is resolved as local wall-clock but converted with `date -u -j -f`, which on macOS parses its input *as UTC* — shifting the window by the zone offset. In CEST a July reconcile dropped the first two hours of 1 July and pulled the last two hours of 31 July in as `2026-08-01` rows. Timestamps are now parsed as local first, which also picks up the DST rule in force on that date rather than the one in force today (v0.5.1).
- **work-reconcile:** the session scan swept subagent transcripts as if they were separate work. They sit one level deeper, at `<slug>/<session-uuid>/subagents/agent-*.jsonl` (775 of 1340 files on the machine measured), and a subagent runs *inside* its parent session, so its timestamps were charged twice; the recursive `find` is now depth-limited to `<slug>/*.jsonl`. Two more inflations went with it: a `.jsonl` is a *resumable* session rather than a work block — the worst case observed spanned 2026-07-15 → 2026-08-09 with 24 259 messages and gap-capped to 3 425 min in one row — so sessions are now split per local calendar day; and minutes are computed from timestamps *inside* the window instead of merely clipping `start`/`end`, which had been charging a July reconcile for nine days of August. Measured effect on one month: 337 blocks / 253.8 h → 170 blocks / 140.9 h, largest single block 3 425 min → 395 min. A new `'long?'` mark flags any day over ~6 h so an unattended agent run cannot be bulk-approved as if the user had been at the keyboard (v0.5.0).
- **work-reconcile:** `project_hint` took the last path segment, which yields `13` for `…/vault-platform/.claude/worktrees/INFRA-13` and `2026` for `…/krato-cluster-2026` — neither a project. It now walks the path right to left, skips numeric and scaffolding segments, and falls back to the repository name (v0.5.0).
- **work-reconcile:** calendar settings hand-written under `sources.google_calendar.reconcile.*` were silently ignored, because the skill reads only the canonical top-level `reconcile.calendar.*`; one live config had its own `exclude_keywords` there and the filter never applied. The nested location is now read as a fallback, and using it prints a warning naming the canonical path (v0.5.0).

- **work-reconcile:** the default `calendar.exclude_keywords` were Czech-only (`oběd`, `lunch`, `dovolená`), so a non-Czech user's `Holiday` / `Vacation` / `Out of office` blocks were reconstructed as work and written into the timesheet as billable time. Defaults now cover the English forms too, and the skill warns that matching is a plain substring test — a short token like `pto` or `off` eats unrelated events (`pto` hits "symptom review") (v0.4.0).
- **work-setup:** the language was asked in step 6 while the skill had been talking to the user since step 1, and in create mode there is no config for "the configured language" to resolve against — a fresh English user got the first five steps in Czech. Language is now step 1's immediate successor, asked bilingually, defaulting to `en` when neither an existing work config, the session-tracker config, nor the session's own language settles it. The no-MCP abort, the per-project override dialog and the scoring options gained English variants (v0.4.0).
- **work-start / work-status / work-end / work-standup:** the missing-config and missing-snapshot aborts fire *before* the config is read, so they had no configured language to be phrased in and were Czech-only literals — an English user hit a dead end in a language they could not read. They now carry both languages inline, the way `work-reconcile` already did (v0.4.0, v0.4.0, v0.4.0, v0.5.0).

### Changed

- **work-*:** every skill in the suite now states, where it reads `config.language`, that the quoted strings below are examples written in `cs` rather than literals to emit verbatim. `work-reconcile` additionally splits its origin-label table into meaning + `cs` rendering and names its approval options by function (**all** / **select** / **skip** / **edit**) instead of by their Czech strings, which the surrounding prose had been re-canonising by referring back to "**Vše**". `work-start` now actually reads `config.language`, which it previously only mentioned in its render step (work-setup, work-start, work-status, work-end v0.4.0; work-standup v0.5.0; work-reconcile v0.4.0, since superseded).
- **repo:** `~/.agents/skills/<name>` now really are live symlinks into a working clone, as AGENTS.md had claimed — all 27 sat as physical copies from the previous day, so a `/work-reconcile` run made to verify a fix silently executed the previous version. AGENTS.md records that `npx skills add` always writes copies and never creates these links, with the loop to re-establish them and the ownership check to run first.
- **repo:** the always-on `kodex` pointer line in the README is now English, matching the English-canonical `skills/kodex/SKILL.md`; `rules/kodex.md` follows it and the Czech original moves to `rules/kodex-cs.md`, mirroring the `references/kodex-cs.md` convention inside the skill.

## 2026-08-24

### Changed

- **retro:** memory is now treated as shared state rather than this session's scratchpad — several agents can run against one project at once, writing the same directory, and concurrent writes overwrite instead of merging. The skill checks for live sessions and does surgical single-entry edits before deleting or compacting anything, and it says outright that a fact which must survive belongs in the repo, which is versioned and reviewable, not in memory, which is neither. Prompted by a real loss: one session's retro compacted away two notes another had written an hour earlier (v0.4.0).
- **conventional-commit:** confirmations are skipped when the commit was already authorised, so a pre-approved commit no longer stops to ask again.

## 2026-08-22

### Changed

- **conventional-commit:** a body is allowed under the size threshold, instead of being suppressed whenever the diff was small enough to look self-explanatory.

## 2026-08-21

### Changed

- **tracker-start:** worktree-isolated sessions get a scratchpad-script fallback. The Bash guard in those sessions rejects the compound `KEY=…; printf … | curl` pattern as too complex to verify, so the skill now writes the whole call into a scratchpad script created with the ordinary file-write tool — its full content visible before anything runs, holding the key *lookup* and never the literal — and executes it as a single plain `bash <script>` (v1.7.0).

## 2026-08-18

### Added

- **dm-catchup:** new skill — catch up on direct messages with a named person across whatever IM the session offers (ClickUp chat, Slack): backend auto-detection by probing available MCP tools, tolerant person matching (dictation-mangled names are flagged, never silently guessed), mandatory thread expansion before claiming absence (ClickUp returns only top-level messages), then summary → open-vs-closed assessment → a claim-audited draft reply sent only after approval, exactly as shown (v1.0.0).
- **resource-busy:** new skill — cross-OS diagnosis of "who blocks this eject/unmount / who holds this file". macOS: `diskutil` stderr names the dissenting PID and its parent while the unified log never does (both verified on macOS 26.6), plus hidden Time Machine snapshot sibling mounts (21 observed at once) that make the visible volume unejectable with an empty `lsof`. Linux: `fuser -vm`/`lsof +f --` as the honest case, and the mount-namespace case where `umount` and `losetup -d` both return 0 while the device stays attached — a `/proc/*/mountinfo` sweep names the holder (verified live: dissented DMG eject; loop device held from an `unshare -m` namespace). Windows section compiled from documentation and explicitly marked untested.
- **mcp-server-adopt:** new skill for adopting a third-party MCP server end to end — candidate sweep, a single `gh api` metadata pass reporting forks next to stars, reading the source instead of trusting the README, a static security audit that must complete **before** any build or test run (`go test` executes the third-party code; a plain `go build` without cgo does not), fork-first installation, credentials kept out of the client config behind a wrapper, scope-aware registration, and verification by a real stdio `initialize` + `tools/list` handshake rather than "the process started". Delegates the MCP-specific security dimensions to `ai-security-skills:mcp-server-review` where that skill is installed, instead of duplicating it.

### Changed

- **tracker-log-entry:** the time window now accepts a "since last entry" anchor ("navázat na poslední záznam"): start = stop of the backend's most recent completed entry, end = now; the anchor entry is echoed in the report and a running timer triggers a question instead of a guess (v1.7.0).
- **tracker-log-entry:** step 6's `KEY=…; printf … | curl` patterns are compound commands, which the Bash guard in worktree-isolated sessions rejects as "too complex to verify"; the skill now documents the remedy — write the whole call into a scratchpad script created with the ordinary file-Write tool (so its full content is visible in the conversation before anything runs), holding the key *lookup* (never the literal), and execute it as a single plain `bash <script>`, keeping the key out of argv (v1.6.2).

### Fixed

- **repo:** frontmatter `argument-hint` values are now quoted strings in the six skills that carried the field at the time (second-opinion, tracker-backfill, tracker-log-entry, tracker-start, work-reconcile, work-standup; `dm-catchup` landed later the same day already quoted). Unquoted `[...]` hints parse as YAML flow sequences: four of the six failed to parse at all and were silently skipped by `npx skills add` — never installable via the CLI — and the other two parsed as one-item lists instead of strings. Claude Code tolerated every variant, which is why nothing looked broken locally.

## 2026-08-13

### Fixed

- **skillify:** the procedure for contributing a generally useful skill to the portable collection now names all three required steps — `skills/<name>/SKILL.md`, a row in the README table, and a dated line in `CHANGELOG.md`. The changelog step has been mandatory since 2026-08-04, but the skill never mentioned it, so anything contributed by following it landed unrecorded (v0.1.1).

## 2026-08-11

### Fixed

- **`tracker-*` / `work-*`:** the two suites advertised overlapping trigger phrases, so an ordinary sentence could land in the wrong skill. `tracker-stop` claimed "end session", "I'm done" and "finished working" — all of which read as the end of the working day (`work-end`) — while `tracker-start` claimed "start session", competing with the morning briefing. `tracker-backfill` and `work-reconcile` both offered to fill missing Toggl time, with nothing in either description saying which one covers the current session and which a past period. Every description now names its own scope ("the running timer", "THIS agent session", "a past period across all sessions") and points at its counterpart, and the ambiguous phrases are gone (`tracker-start`, `tracker-stop`, `tracker-backfill` v1.6.1; `work-start`, `work-end`, `work-reconcile` v0.3.1).

## 2026-08-10

### Added

- **decision-analysis:** methodology for a context-anchored decision analysis that ends in a verdict — decision rules written and dated before any evidence arrives, every load-bearing claim sourced and dated, and the durable layer kept separate from a perishable dated snapshot.

### Changed

- **launchpad-fix:** reworked from "re-register apps with `lsregister`" into a two-layer triage. A broken or empty Spotlight index — including an `mds` daemon wedged by system overload, where `mdutil -E` reports success while nothing gets indexed — now gets diagnosed and repaired separately from genuine Launch Services gaps, since `lsregister` does nothing for the former. A system-health check (load, zombies, swap) runs first because an overloaded machine cannot finish reindexing at all, and the Launchpad/Dock reset is skipped on macOS 26+, where Launchpad no longer exists.

### Fixed

- **launchpad-fix:** step 4 now states its two preconditions up front — the repairs all need `sudo` an agent cannot supply, and a volume near capacity makes every one of them a no-op. Measured on a Data volume at 98 %: a freshly restarted `mds` kept 9–28 `mdworker` processes busy for six minutes without the index growing by a single item.
- **launchpad-fix:** the step-3 triage measured `kMDItemFSSize > 0` against `/Applications`, which counts neither the app bundles (directories are skipped) nor their contents (Spotlight never indexes bundle interiors) — so it reported a healthy index as broken. Measured on macOS 26.6: 164 hits against 35 348 entries on disk, on a machine whose app index was in fact complete. Step 3 now counts apps via `com.apple.application-bundle`, compares files against a folder the user knows, and names the two false alarms that mimic a dead file index (iCloud `dataless` placeholders, Spotlight Privacy exclusions). Step 4 gained a check separating a wedged `mds` from one that is merely behind, so its repairs no longer throw away in-flight indexing work.
- **tracker-log-entry:** the macOS local-time → UTC conversion prescribed `date -j -f "<fmt>" "<in>" -u +<outfmt>`, where the trailing `-u` is not parsed as a flag: the output format was ignored and the skill fed the API a localized date string it rejected as `Invalid time format`. Replaced with an epoch round-trip, plus the two variants that fail *silently* (`-u` in front parses the input as UTC; omitting `-u` prints local time with a `Z` suffix) and why `%S` has to stay in the input format (v1.6.1).
- **code-review:** in a worktree-isolated session the findings file now goes into the worktree's own `docs.local/code-reviews/`, and copying it back into the main checkout is offered at the end of the CR — the isolation guard blocks writes into the main checkout's `docs.local/`, so the findings had nowhere to land, and a worktree copy disappears with the worktree. Reading a PR's new commits between rounds now counts as the next round's verification too: the round-N timer starts before that delta is read, so the substantive work no longer falls outside every entry while the formal round logs a misleading minute.

## 2026-08-07

### Added

- **repo:** the collection is now MIT-licensed — root `LICENSE` plus `license: MIT` in every skill's frontmatter, so the license travels with per-skill copies made by `npx skills add`.

## 2026-08-04

### Added

- **work-standup:** the recap window can now be derived from `standup.default_window: "prev_standup"` plus the `schedule` block (days/time/timezone) in the work config — no more passing `--since` by hand on every scheduled stand-up (v0.4.0).

## 2026-08-01

### Added

- **kodex:** the thinking codex ships as a regular skill with an always-on pointer; the English text in `SKILL.md` is canonical, the Czech original lives in `references/kodex-cs.md`.
- **tracker-backfill:** splits the session transcript into activity blocks, so multi-day sessions backfill as separate entries.

### Changed

- **repo:** all skills moved into `skills/` (the Agent Skills standard layout) — local-path installs and symlinks created before this date point at the old locations and need re-creating.
- **repo:** skill texts are harness-neutral (actions instead of tool names, no plugin-era paths), making the collection usable beyond Claude Code; `npx skills add kratocz/skills` is the supported install path.

## 2026-07-21

### Added

- **repo:** initial import of the skills collection (Antigravity-era flat layout) with the README skills table; kodex gains the explicit Git rule (no `git add .`).
